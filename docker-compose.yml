version: '3.8'

services:
  backend:
    build: ./backend
    expose:
      - "8080"

  frontend:
    build: ./frontend
    expose:
      - "80"

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend